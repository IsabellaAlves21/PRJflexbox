import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    justifyContent:'space-around',
    borderStyle:'solid',
    borderWidth:3,
    borderBottomWidth: 1,
    padding: 20,
  }
});